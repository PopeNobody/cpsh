#include <fmt.hh>
#include <coinfwd.hh>

namespace fmt {
#if 0
  ostream &fmt::streamable_tag::stream(ostream &rhs) const
  {
    cout << "somebody forgot to define their stream func." << endl;
    return cout;
  };
#endif
};



